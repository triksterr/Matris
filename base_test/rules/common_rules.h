#pragma once
// file: common_rules.h

// Правила без приявзки - могут использоваться всеми

#include "rules.h"

//! ГСЧ
//std::random_device rd;
//std::mt19937 gen(rd());
//
// // Define distribution for integers from 1 to 6 (inclusive)
//std::uniform_int_distribution<> distrib(1, 6);
//
// // Generate a random number
//std::cout << "Rolled a " << distrib(gen) << "\n";